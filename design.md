# AXPORT 디자인

홈 화면과 대시보드 앱의 디자인을 구분한다. 세부 디자인은 추후 수정한다. 기능·판정 기준은 `AXPORT_초기개발명세.md`를 따른다.
참고 이미지: `home_screen_reference.png`. 색감·여백·카드 구성·정보 위계를 참고하며, 로고·문구·실적 수치·고객사는 그대로 사용하지 않는다.

## 홈 화면: 색감과 UI

- 첫 화면은 짙은 네이비에서 블루로 이어지는 배경과 흰색 제목을 사용한다. 본문은 흰색과 옅은 회색 배경으로 전환한다.
- 초기 색상 제안: 네이비 `#061A40`, 강조 블루 `#326BFF`, 흰색 `#FFFFFF`, 옅은 배경 `#F5F7FC`, 본문 `#202938`. 이미지에서 추출한 정확한 값은 아니며 공통 스타일에서 조정한다.
- 주요 행동 버튼은 블루 계열, 보조 버튼은 흰색 또는 테두리형으로 구분한다. 어두운 배경에서도 글자와 버튼이 명확히 보이게 한다.
- 한글·영문 지원 공통 폰트와 대체 폰트를 지정한다. 제목·본문·보조문구의 크기와 행간은 공통 스타일로 관리한다.
- 같은 역할의 버튼은 높이·여백·모서리·글자 크기를 통일하고 기본 높이는 40px 이상으로 한다.
- 본문은 중앙 정렬된 최대 너비 안에 배치하고, 섹션 사이에 충분한 여백을 둔다. 카드는 얇은 테두리·완만한 모서리·약한 그림자로 정돈한다.
- 긴 문구·한글·영문·화면 크기 변경에도 겹치거나 잘리지 않게 한다. 좁은 화면에서는 다열 카드를 단일 열로 전환한다.

## 홈 화면: 구성

1. **상단 헤더:** 왼쪽에 서비스 로고, 오른쪽에 ‘대시보드 시작’ 버튼을 둔다.
2. **첫 화면:** 네이비·블루 배경에 서비스 목적을 설명하는 짧은 제목과 보조문구, 중앙 3D 모델 또는 애니메이션을 배치한다. 버튼과 문구를 가리지 않도록 한다.
3. **서비스 소개:** 스크롤 이후 흰색·옅은 회색 배경에서 ‘기업 데이터 업로드 → HS 코드·대상국 확인 → 분석 결과 확인’ 흐름을 간결하게 설명한다.
4. **분석 기능 소개:** 규제·시장성·가격·물류·안정성 5개 영역을 짧은 카드로 소개한다. 세부 지표와 복잡한 분석 차트는 대시보드 앱에서 제공한다.
5. **하단 진입 영역:** 네이비 배경의 간결한 안내와 ‘대시보드 시작’ 버튼을 배치한다. 푸터는 필요한 서비스 정보만 표시한다.

## 홈 화면: 동작과 콘텐츠

- ‘대시보드 시작’ 버튼은 대시보드 앱으로 연결한다. 진입 로딩은 실제 준비 상태와 연결하며 불필요한 고정 대기시간을 넣지 않는다.
- 3D·애니메이션 로딩 실패 시 정적 이미지나 배경으로 대체한다. 움직임 줄이기 설정을 존중한다.
- 서비스 실적·정확도·고객사·후기 등은 검증된 내용이 있을 때만 표시한다. 예시 화면과 가상 수치는 예시임을 명시한다.
- 참고 이미지의 뉴스·후기·고객사 등 모든 섹션을 재현할 필요는 없다. 초기에는 위 구성만 적용한다.

## 대시보드 앱: 바탕화면과 창

- 홈과 구분된 배경화면에 업로드한 엑셀 파일을 아이콘과 파일명으로 표시한다.
- 아이콘은 드래그로 이동하고 놓을 때 일정한 행·열 격자에 정렬한다. 아이콘과 파일명이 겹치지 않게 한다.
- 아이콘은 더블클릭으로 열며 키보드 조작도 지원한다. 휴지통 아이콘을 제공해 불필요한 파일을 바탕화면에서 제거한다.
- 분석 창은 이동·크기 조절이 가능하고 오른쪽 책갈피 탭은 창과 함께 이동한다.
- 앱의 폰트·버튼·간격은 공통 스타일로 통일한다. 기본 버튼 높이는 40px 이상으로 하며 창을 줄여도 글자·버튼이 겹치거나 잘리지 않게 한다.

## 대시보드 앱: 분석 결과

- 메인에는 **규제(관문), 시장성(시장규모·성장률), 가격(관세·환율), 물류(운송), 안정성(변동률)** 5개 요약 항목만 표시한다.
- 각 항목은 핵심 결과 1개와 짧은 설명 1줄로 구성한다. 상세 수치·차트·계산 근거·출처는 오른쪽 책갈피형 하위 탭에 배치한다.
- 기업·제품·HS 코드·대상국·기준일은 상단에 간결하게 표시한다. 자료 부족·평가 보류는 색상과 문구로 구분한다.
- ‘가중치 설정’에서 자체 채점 기준의 기본 가중치를 확인·변경·복원할 수 있게 한다. 사용자 설정 적용 여부를 표시하고 규제 관문 상태는 별도로 유지한다.
- ‘보고서 다운로드’ 버튼으로 대시보드 결과와 분석 내용을 내려받게 한다. 생성 중·완료·실패 상태를 표시한다.
