# AXPORT 초기 개발 명세

- 작성일: 2026-09-21
- 수정일: 2026-09-23
- 문서 버전: 0.3
- 상태: 기획 초안 — 사용자 확정 요구사항과 개발 제안을 구분함
- 목적: 화면과 기능을 계속 개선하더라도 유지할 데이터·판정·보안·공통 UI 원칙을 정의한다.
- 적용 범위: 향후 개발·개선 작업. 이 문서 자체가 기존 코드 수정, 배포, 데이터 삭제를 승인하는 것은 아니다.

## 1. 서비스 목적과 확정 요구사항

기업의 엑셀 데이터를 업로드하고 제품의 HS 코드를 확인하여 수출적합도를 분석한다. 결과와 판단 근거를 같은 분석 작업공간에서 확인할 수 있어야 한다.

### 사용자 확정 요구사항

1. 서비스 소개용 홈과 분석용 대시보드 앱을 분리한다.
2. 홈 첫 화면은 네이비 배경과 중앙 3D 모델 또는 애니메이션으로 구성한다. 구체적인 홈 디자인은 추후 결정한다.
3. 홈을 아래로 스크롤하면 흰 배경의 정돈되고 포멀한 UI로 이어진다.
4. 홈 우측 상단에 대시보드 앱 진입 버튼을 둔다.
5. 앱 진입 과정에 로딩 애니메이션을 제공한다.
6. 주요 기능은 대시보드 앱에 배치한다.
7. 앱에는 바탕화면이 있으며, 엑셀 업로드와 콘텐츠 아이콘 진입을 지원한다.
8. 분석 창은 이동과 크기 조절이 가능하다. Mac 외형을 그대로 복제할 필요는 없다.
9. 주요 기능은 기업 데이터와 HS 코드에 근거한 수출적합도 분석이다.
10. 분석 창 오른쪽에 책갈피 형태의 세로 탭을 두고 관세·수출입실적 등 판단 근거별 화면을 전환한다.
11. 각 근거 화면에는 수치·차트·설명과 근거를 제공한다.
12. 새 폴더에서 프로젝트를 처음부터 구축한다. 기존 프로젝트 재사용이나 기존 경로 호환은 전제하지 않는다.
13. 디자인 시안은 ‘홈 화면 디자인’과 ‘대시보드 앱 디자인’ 두 영역으로 나누어 작성·검토·개선한다.

14. Python·Flask 환경에서 구현한다.
15. 평가 영역은 규제(관문), 시장성(시장규모·성장률), 가격(관세·환율), 물류(운송), 안정성(변동률)으로 구성한다.
16. 팀 자체 채점 기준과 기본 가중치를 사용하며 사용자가 가중치를 변경할 수 있게 한다. 규제 관문은 별도로 적용한다.
17. 업로드한 엑셀 파일은 앱 바탕화면에 아이콘으로 표시한다. 아이콘은 격자에 맞춰 이동하고 더블클릭으로 열며 휴지통을 통해 바탕화면에서 제거할 수 있다.
18. 대시보드 결과와 분석 내용을 보고서로 다운로드할 수 있게 한다.

## 2. 초기 구현 제안과 미확정 사항

이 절의 값은 개발 시작을 위한 제안이며 사용자 확정 사항으로 취급하지 않는다. 변경 시 결정 이력을 남긴다.

| 항목 | 초기 제안 | 상태 |
|---|---|---|
| 기본 언어 | 한국어, 영어 확장이 가능한 문구 구조 | 제안 |
| 기본 표시 시간대 | Asia/Seoul | 제안 |
| 경로 | 홈 `/`, 분석 작업공간 `/app` | 제안 |
| 앱 진입 방식 | 같은 브라우저 탭에서 이동 | 제안 |
| 초기 업로드 형식 | `.xlsx` | 제안 |
| 초기 평가 범위 | 반도체 제품군과 대상국을 제한하여 시작 | 구체적 범위 미정 |
| 초기 관세 분석 | 확보한 미국 관세표의 적용 범위를 검증한 뒤 미국부터 구현 | 제안 |
| AI 기능 | 계산 결과와 근거의 설명·요약 보조 | 제안 |
| 모바일 대응 | 좁은 화면에서 단일 창 중심 표시 | 제안 |
| 계정·권한 | 실제 기업 자료 수용 전 기업별 접근 통제 마련 | 방식 미정 |
| 요금제·결제 | 핵심 판정 흐름과 분리 | 도입 여부 미정 |

### 개발 전에 결정할 항목

- 초기 지원 제품군, 목적국, 규제 검토 범위
- 시연용 서비스인지 실제 기업 사용을 받는 서비스인지
- 로그인 방식, 조직·사용자 역할, 데이터 보관 기간
- 파일 크기·행 수·시트 수·처리시간 제한
- Python·Flask 버전, 프런트엔드 구성, 데이터베이스·파일 저장소·배포 환경
- 5개 영역의 세부 계산식·기본 가중치·임계값 및 검토·승인 절차
- 규제 점수의 합산 여부, 사용자 가중치 조절 범위·합계 검증 방식
- 안정성의 변동률 대상 지표·기간·주기·계산 방식
- 휴지통 복원·비우기·영구 삭제 및 관련 분석 결과 보관 정책
- 보고서 파일 형식(PDF 등), 포함할 화면·차트 범위와 다운로드 제한
- 상세 탭의 최종 구성과 작은 화면 전환 기준

## 3. 평가 단위와 공통 식별자

### 평가 단위

개별 거래 평가의 기본 단위는 다음과 같이 설계한다.

> 기업 + 제품·모델 + 목적국 + 거래 조건 + 평가 기준일

- HS 코드는 제품을 연결하는 분류 정보이며 단독으로 종합 판정을 확정하는 입력이 아니다.
- 대상국 선택을 기본 분석 흐름에 포함한다.
- 구매자·최종사용자·최종용도·가격·수량·납기 등이 없으면 가능한 분석만 제공하고 부족한 영역을 표시한다.
- 시장 탐색 분석과 구체적인 거래 평가를 구분한다. 거래 조건이 없는 탐색 결과를 완결된 거래 판정으로 표시하지 않는다.
- 평가 기준일과 실적 분석 시작·종료일을 별도 필드로 둔다.

### 데이터 연결

- 기업, 제품, 거래처, 거래, 데이터셋, 평가 실행에 고유 ID를 부여한다.
- 이름·표시명은 식별자로 사용하지 않는다.
- HS 코드는 문자열로 저장하고 분류 체계·버전·국가별 세번을 구분한다.
- 통화·수량 단위·금액 배율을 값과 함께 저장한다.
- API·파일의 국가명은 표준 국가 코드에 연결하되 원본 값도 보존한다.

## 4. 언어·날짜·숫자 표시

- 메뉴·오류·판정 설명·차트·보고서 문구를 번역 리소스로 분리한다.
- 용어사전에서 ‘수출적합도’, ‘추가 확인 필요’, ‘평가 보류’, ‘자료 부족’ 등의 표현을 관리한다.
- 언어 변경과 통화·환율·분석 조건 변경을 분리한다.
- 규정 원문과 번역을 구분하고 원문 위치를 유지한다.
- 시스템 이벤트 시간은 UTC로 저장하고 화면 시간대에 맞춰 변환한다.
- 규정 시행일 등 날짜 자체가 의미인 값은 시간대 변환으로 날짜가 바뀌지 않도록 날짜형으로 관리한다.
- 기본 날짜 표시는 `YYYY-MM-DD`로 제안한다.
- 금액 계산과 화면 반올림을 분리한다. 통화별 정밀도·반올림 정책을 명시한다.
- 모든 지표에 통화·단위·기간을 표시한다.
- 실제 값 0, 미입력, 미평가, 해당 없음, 조회 실패를 구분한다.
- 번역 누락 시 기본 언어로 표시하되, 미번역 내용을 식별할 수 있게 한다.

## 5. 기업 데이터 업로드 규격

### 표준 시트 제안

| 시트 | 주요 항목 | 활용 |
|---|---|---|
| 제품정보 | 제품 ID, 모델, 제품군, HS·버전, 제조국, 사양·사양서 참조 | 제품 식별·규정 대조 |
| 수출예정거래 | 제품 ID, 목적국, 거래처, 최종사용자·용도, 수량, 가격, 통화, 납기, 거래 조건 | 평가 대상 거래 |
| 수출실적 | 거래일, 제품·고객·국가, 수량·단위, 금액·통화, 취소·반품 | 실적·집중도 |
| 원가·비용 | 제품 원가, 통화, 운송·보험·포장비, 부담 주체, 결제 조건 | 수익성·환율 |
| 재고·생산 | 가용·예약 재고, 기존 수주, 생산 일정, 검사기간, 공급 가능 수량 | 납기·공급 |
| 물류 | 출발·도착지, 수단, 예정·실제 일자, 운임, 견적 유효기간 | 물류비·이행 능력 |
| 거래처 | 법인명·별칭, 주소, 국가, 등록번호, 최종사용자와의 관계 | 거래 상대 대조 |
| 증빙목록 | 관련 제품·거래 ID, 문서 종류·번호, 발행·만료일, 첨부 참조 | 판정·원산지·허가 근거 |

### 업로드 원칙

- 양식에 `template_version`을 둔다. 향후 컬럼 변경 시 구버전 지원 범위를 명시한다.
- 전체 공통 필수 항목과 분석 영역별 필수 항목을 구분한다.
- 원본 파일을 보존하고 정규화된 데이터와 사용자 보정 이력을 별도 저장한다.
- 업로드 → 검증 → 매핑·단위 확인 → 분석 순서로 처리한다.
- 숫자·날짜·중복·범위·제품 및 거래처 연결 오류를 검사한다.
- 입력 누락을 임의의 값이나 0으로 채우지 않는다.
- 컬럼 자동 매핑은 후보를 제시하고 불명확한 연결은 사용자 확인을 받는다.
- Excel 매크로·수식을 실행하지 않는다. 수식 셀의 허용·읽기 정책은 별도로 명시한다.
- 파일 형식·크기·압축해제 크기·셀 수·처리시간 제한을 서버에서 검증한다.
- 파일명은 표시 정보로 취급하고 저장 경로는 서버가 생성한다.
- 다운로드 표·CSV·보고서에는 수식 주입 방어를 적용한다.

## 6. 판정 결과와 근거 구조

### 5개 평가 영역 — 사용자 확정

| 영역 | 판단 범위 | 적용 원칙 |
|---|---|---|
| 규제 | 수출규제·허가 등 필수 조건 | 관문으로 평가하며 다른 점수나 가중치로 미충족·미확인을 상쇄하지 않음 |
| 시장성 | 시장규모·성장률 | 대상국·품목·분석 기간을 명시 |
| 가격 | 관세·환율 | 적용 조건·통화·환율 기준일을 명시 |
| 물류 | 운송 | 운송 수단·경로·기간·비용 중 사용하는 지표와 근거를 명시 |
| 안정성 | 변동률 | 대상 지표·기간·주기·계산 방식은 별도 확정 |

시장성의 성장률과 안정성의 변동률은 목적과 계산 방법을 구분한다. 안정성을 임의로 국가위험·신용등급과 동일시하지 않는다. 세부 공식·점수·임계값은 아직 확정하지 않았으며 AI가 임의로 채우지 않는다.

### 자체 채점 기준과 사용자 가중치

- 팀이 검토한 채점 기준·기본 가중치를 버전으로 관리한다. 사용자는 적용 가중치를 변경할 수 있으나 채점 공식·규제 조건은 변경하지 않는다.
- 기본 가중치로 분석하며 사용자가 변경하거나 기본값으로 복원할 수 있게 한다.
- 규제 관문은 점수 합산과 별도로 유지한다. 규제 점수를 합산에도 사용할지, 조절 가능한 항목·범위·합계 처리 방식은 채점 기준 확정 시 명시한다.
- 서버에서도 가중치를 검증한다. 음수·비수치·합산 대상 전체가 0인 입력은 거절하며 합계 불일치 처리 방식을 안내한다.
- 적용 가중치·기본값/사용자 설정 구분·기준 버전을 결과와 보고서에 기록한다. 변경 후 재계산은 새 평가 실행으로 저장한다.
- 자료 부족 영역을 0점으로 치환하거나 나머지 가중치에 조용히 재배분하지 않는다. 부분 평가·종합 평가 보류 기준은 별도 확정한다.

### 결과를 세 층으로 분리

| 구분 | 내용 |
|---|---|
| 필수 조건 검토 | 규제·허가·원산지 등 확인된 조건, 미충족 조건, 추가 확인 사항 |
| 사업적 적합도 | 시장성·가격·물류·안정성의 계산 가능한 영역 평가 |
| 근거 충족 상태 | 필수 입력 확보 여부, 자료 유효성, 출처·기준일 |

### 판정 원칙

- 사업성 점수로 필수 조건 미충족이나 핵심 규제 미확인을 상쇄하지 않는다.
- 자료가 부족한 상태와 실제 위험이 낮은 상태를 구분한다.
- 평가 불가 영역이 있으면 그 이유와 필요한 입력을 반환한다.
- 점수는 성공 확률·법적 허가 여부와 구분한다.
- 가중치·임계값·기업 정책은 중앙 설정으로 관리하고 근거·검토 상태를 기록한다.
- 모델이 임의로 점수 기준이나 규정 조건을 생성하여 확정하지 않는다.
- 동일 비용·위험이 여러 항목에서 중복 반영되지 않도록 검토한다.
- 비교 결과는 평가 범위·데이터 충족 상태가 다른 경우 차이를 표시한다.

### 평가 실행마다 보존할 정보

- 입력 데이터셋·사용자 수정 버전
- 기업·제품·목적국·거래 조건과 분석 기간
- 규칙·계산 엔진 버전 및 적용 가중치·기본값/사용자 설정 구분
- 사용한 외부 자료의 버전·기준일·조회 조건
- 결과, 계산식, 근거 위치, 누락 입력, 후속 조치
- 실행 시각·실행 주체

입력·규칙·외부 자료가 변경되면 새 평가 실행으로 저장한다. 과거 결과를 재조회할 때 최신 자료로 조용히 다시 계산하지 않는다. 화면과 보고서는 같은 평가 실행을 참조한다.

## 7. 외부 데이터·규정 파일 관리

### 사용자 확보 목록 — 아직 실물·응답 미검증

| 자료 | 예정 용도 | 확인할 사항 |
|---|---|---|
| 관세청 품목별 국가별 수출입실적 | 한국 수출 추이 | 승인·응답·단위·기간 |
| 관세청 관세환율정보 | 한국 통관 환산 | 관세율·결제환율과 구분 |
| 해양수산부 선박운항정보 | 물류 현황 보조 | 실제 화물 납기·운임과 구분 |
| ECOS·KOREAEXIM | 환율 자료 | 서비스 종류·주 출처 선정 |
| UN Comtrade | 대상국 수입시장 | 조회 범위·품목 버전·자료 누락 |
| WTO | 관세·무역 지표 보완 | 발급 키의 대상 서비스·조회 가능 항목 |
| 국가법령정보 API | 규정·변경 확인 | 본문·별표 접근 범위 |
| 전략물자수출입고시 HWP | 판정 기준 원문 | 시행일·버전·표 및 주석 추출 정확성 |
| OECD 국가위험분류 PDF | 국가위험 참고 | 기준일·분류 의미·미분류 상태 |
| 거래 제한 대상 CSV | 거래 상대 대조 | 제공기관·관할·목록 종류·기준일 |
| 미국 관세표 XLSX | 미국 관세 검토 | 개정판·세율 조건·주석·적용일 |
| OpenAI API | 설명·요약 보조 | 연결 상태·전송 범위·비용 제한 |

### 수집·저장 정책

- 제공기관별 연결 모듈을 분리하고 출력 구조를 표준화한다.
- 출처 URL, 자료 기준일, 수집일, 적용 시작·종료일, 파일 해시와 버전을 저장한다.
- 규정의 발표일과 시행일을 구분한다.
- API 호출의 시간 초과·재시도·호출량 제한·캐시 정책을 설정한다.
- 연결 실패, 자료 없음, 권한 없음, 오래된 저장 자료 사용을 구분해 반환한다.
- 실제 자료·샘플 자료·사용자 입력·가정값을 구분하고 화면·보고서에 표시한다.
- 장애 시 샘플 자료로 자동 대체하여 실제 결과처럼 표시하지 않는다.
- 공식 파일을 파싱한 후 원문 대조·검토를 거쳐 판정 규칙에 반영한다.
- 원문에서 추출한 미검토 규칙은 운영 판정에 사용하지 않는다.
- 미국 관세표를 다른 국가 관세 자료로 사용하지 않는다.
- 자료 이용조건·재배포 범위와 갱신 담당을 기록한다.

## 8. 홈·작업공간·책갈피 탭

### 디자인 시안의 두 영역 — 사용자 확정

홈 화면과 대시보드 앱은 목적과 정보 밀도가 다르므로 별도 시안으로 다룬다. 한쪽의 시안을 개선할 때 다른 쪽까지 함께 확정할 필요는 없다.

| 시안 | 목적 | 포함 범위 |
|---|---|---|
| 홈 화면 디자인 | 첫인상, 서비스 이해, 앱 진입 | 네이비 히어로, 중앙 3D·애니메이션, 스크롤 이후 흰 배경 콘텐츠, 상단 앱 진입 버튼 |
| 대시보드 앱 디자인 | 기업 자료 업로드, 분석 결과와 근거 탐색 | 바탕화면·아이콘, 업로드·검증, 이동·크기 조절 창, 종합 결과, 오른쪽 책갈피 탭, 근거별 수치·차트·설명 |

앱 진입 로딩은 두 영역의 연결 화면으로 정의하고, 로딩 시각 스타일은 앱 시안에서 다루되 홈에서 전환되는 흐름까지 확인한다.

다음은 시안 작성 시 사용할 제안 기준이다.

- 공통 시각 규칙은 `design.md`를 따른다. 홈과 앱의 상세 디자인은 구분한다.
- 홈: 큰 시각 요소와 여백을 활용하되 스크롤 이후에는 포멀하고 정돈된 정보 배치.
- 앱: 장시간 분석에 적합한 가독성, 수치 비교, 명확한 창·탭 상태를 우선.
- 앱 시안에는 빈 바탕화면, 업로드·검증, 분석 중, 결과, 근거 탭, 자료 부족·오류 상태를 포함.
- 시안 속 가상 수치·판정은 설명용 예시임을 표시하고 실제 검증 결과로 취급하지 않음.

### 공통 동작

- 홈과 앱의 화면 상태를 분리한다.
- 로딩 애니메이션은 실제 준비 상태와 연결하고 불필요한 고정 대기시간을 넣지 않는다.
- 준비 실패 시 이유·재시도 경로를 표시한다.
- 3D 자산을 불러오지 못하면 정적 대체 화면으로 진입할 수 있어야 한다.
- 앱 바탕화면에 업로드·최근 분석·기업 데이터·보고서 진입점을 제공한다.

### 바탕화면 파일 아이콘·휴지통

- 업로드한 엑셀 파일을 앱 배경화면 위에 아이콘과 파일명으로 표시한다. 검증 중·오류·분석 가능 상태를 구분한다.
- 파일 ID에 연결된 아이콘을 드래그로 이동하며 놓을 때 가장 가까운 사용 가능한 행·열 격자 칸에 정렬한다. 같은 칸에 겹치거나 화면 밖에 배치되지 않게 한다.
- 아이콘 배치는 사용자별로 저장하고 화면 크기 변경 시 보정한다. 격자 간격·정렬 시작점은 공통 UI 설정으로 관리한다.
- 단일 클릭은 선택, 더블클릭은 파일 내용·검증 결과를 여는 동작으로 구분한다. 파일을 열기만 해서는 분석을 자동 재실행하지 않는다.
- 휴지통 아이콘을 제공한다. 제안 동작은 파일을 휴지통으로 드래그하거나 선택 후 ‘휴지통으로 이동’을 실행하는 방식이며 휴지통은 더블클릭으로 연다.
- 휴지통 이동은 우선 바탕화면에서 제거하는 동작으로 정의한다. 원본·연결된 분석 결과의 영구 삭제로 간주하지 않으며 복원·비우기·보관 기간은 별도 확정한다.
- 아이콘 선택·열기·이동·휴지통 이동은 키보드로도 수행할 수 있게 한다.

### 메인 결과·보고서 다운로드

- 메인 결과는 규제·시장성·가격·물류·안정성 5개 요약으로 제한하고 상세 내용은 책갈피 탭에서 제공한다.
- 가중치 설정과 보고서 다운로드 진입점을 제공하되 메인에 상세 설정·데이터를 나열하지 않는다.
- 보고서에는 대시보드 요약, 영역별 분석, 평가 대상·기준일, 적용 가중치, 계산 근거·출처·버전, 자료 부족·미확인 사항을 포함한다.
- 화면과 보고서는 같은 평가 실행을 사용한다. 다운로드 시 최신 데이터로 재계산하거나 현재 설정으로 과거 결과를 덮어쓰지 않는다.
- 보고서 형식과 차트·화면 재현 범위는 별도 확정한다. 생성 중·완료·실패와 재시도 경로를 제공하고 다운로드 권한을 검사한다.

### 창 관리자

- 이동·크기 조절을 기본 기능으로 지원한다.
- 최소화·최대화·닫기와 창 전환 방식은 공통 컴포넌트로 구현한다.
- 최소 창 크기를 정하고 창의 조작부가 화면 밖에 갇히지 않도록 한다.
- 창 위치·크기를 저장하되 화면 크기가 달라지면 보정한다.
- ‘창 배치 초기화’를 제공한다.
- 동일 분석 창을 중복 실행할지 기존 창을 활성화할지 일관된 정책을 둔다.
- 좁은 화면은 단일 창 전환 방식으로 대응하는 것을 기본안으로 검토한다.

### 책갈피 탭

- 오른쪽 책갈피는 분석 창에 부착되어 함께 이동한다.
- 선택 탭은 색상 외에 형태·텍스트·접근성 상태로도 구분한다.
- 창 높이가 작으면 탭 영역을 스크롤할 수 있게 한다.
- 한 창의 기업·제품·목적국·평가 실행은 탭 전환 시 유지한다.
- 탭 전용 필터는 공통 조건과 구분해 표시한다.
- 탭 구성안: 종합판정, 규제, 시장성, 가격, 물류, 안정성. 이름·순서는 별도 확정하며 수출입실적은 시장성, 관세·환율은 가격 등 해당 영역의 상세 근거로 연결한다.
- 데이터 품질 상세를 별도 탭 또는 데이터 관리 화면에 배치하되 종합 화면에 누락 상태를 표시한다.

### 상세 화면 공통 구성

1. 평가 대상과 기준일
2. 영역별 결과·핵심 수치
3. 차트·상세 표
4. 판단 이유·계산식·근거 출처
5. 미확인 사항·필요한 추가 자료·후속 조치

## 9. 사용자 설정과 접근성

- 언어·테마·창 배치 등 개인 UI 설정을 저장한다.
- 공용 PC를 고려해 저장된 사용자 설정을 초기화할 수 있게 한다.
- 키보드로 주요 기능·창·책갈피를 조작할 수 있게 한다.
- 활성 창·탭과 키보드 초점을 명확히 표시한다.
- 애니메이션 줄이기 설정을 지원한다.
- 차트는 표 형태로도 확인할 수 있게 한다.
- 색상만으로 정상·주의·미평가를 구분하지 않는다.
- 필터 변경 후 이전 결과를 최신 결과처럼 표시하지 않는다. 로딩·오류·빈 상태를 구분한다.

## 10. 계정·보안·보관 정책

- 시연 데이터 환경과 실제 기업 데이터 환경을 구분한다.
- 실제 기업 자료 수용 전 로그인·기업별 소유권·역할별 접근 정책을 확정한다.
- 조회·수정·삭제·보고서 다운로드마다 서버에서 접근 권한을 검사한다.
- 언어·화면 모드·클라이언트 설정값을 보안 권한으로 사용하지 않는다.
- API 키·서버 비밀값은 서버 환경설정에만 보관하고 Git·브라우저·로그에 노출하지 않는다.
- 개발·테스트·운영 키를 분리하고 `.env.example`에는 변수명과 설명만 둔다.
- 운영 비밀키 미설정 시 고정된 개발 키로 운영되지 않도록 한다.
- 외부 AI 전송 대상·제외 항목을 정의하고 최소한의 필요한 정보만 전송한다.
- 원본 파일·정규화 데이터·보고서·로그·백업별 보관 및 삭제 정책을 정한다.
- 중요한 변경·분석·다운로드의 감사 이력을 남기되 원본 민감정보를 로그에 넣지 않는다.
- 시연을 위한 세션 저장을 장기 기업 계정 저장과 혼동하지 않는다.

## 11. 구현 구조와 환경 설정

### 구현 환경 — 사용자 확정

- 서버는 Python·Flask로 구현한다. 업로드 검증·지표 계산·판정·보고서 생성은 서버 계층에서 처리한다.
- Python·Flask 버전, 의존성 관리 도구, 프런트엔드 방식, 데이터베이스·배포 환경은 별도 확정한다. Flask 채택만으로 프런트엔드 프레임워크까지 확정하지 않는다.

### 계층 분리

- UI·창 관리 / API / 업로드 검증 / 지표 계산 / 판정 규칙 / 외부 데이터 연결 / 보고서 / AI 설명을 분리한다.
- 판정·계산 로직을 화면 JavaScript에만 구현하지 않는다.
- 화면과 보고서는 동일한 서버 계산 결과를 사용한다.
- 기업·데이터셋·평가·규칙 메타데이터는 영속 저장하고, 원본 파일은 별도로 보관한다.
- 새 프로젝트의 구조·공통 설정부터 구축한다. 이전 프로젝트의 코드·의존성·설정은 자동으로 복사하거나 전제하지 않는다.

### 중앙 설정 항목

- 실행 환경과 서비스 주소
- 기본·지원 언어, 표시 시간대, 통화·단위 정책
- 업로드·파일 처리·API 요청 제한
- 저장 경로·보관 정책·백업 정책
- 외부 자료 갱신·캐시·최대 허용 경과시간
- 판정 규칙·양식·계산 엔진 버전
- 기능 활성화 여부와 데모 모드
- AI 모델 설정·요청 제한·비용 상한

설정값을 UI·서버·보고서에 중복 하드코딩하지 않는다. 사용자가 변경 가능한 설정과 운영자 전용 설정을 구분한다.

## 12. 초기 완료 검증 기준

- [ ] Flask 환경에서 업로드·분석·보고서 다운로드 흐름이 동작한다.
- [ ] 파일 아이콘의 격자 이동·배치 복원·더블클릭·휴지통 이동이 동작한다.
- [ ] 휴지통 이동만으로 원본·기존 평가 결과가 영구 삭제되지 않는다.
- [ ] 메인은 5개 영역으로 요약되고 상세 근거는 책갈피 탭에서 확인된다.
- [ ] 기본·사용자 가중치 적용과 복원이 동작하고 규제 관문은 가중치 변경으로 해제되지 않는다.
- [ ] 잘못된 가중치는 거절되고 변경 후 새 평가 실행에 적용값이 보존된다.
- [ ] 보고서에 화면과 같은 결과·가중치·근거·자료 부족 상태가 포함된다.

- [ ] 홈에서 앱 진입, 로딩 성공·실패·재시도가 동작한다.
- [ ] 창 이동·크기 조절·책갈피 전환 후에도 동일 평가 맥락이 유지된다.
- [ ] 새로고침·작은 화면·창 배치 복원 시 조작부에 접근할 수 있다.
- [ ] 정상·오류·누락·중복 엑셀의 검증 결과가 구분된다.
- [ ] 0과 미입력, 해당 없음과 미평가가 구분된다.
- [ ] 외부 API 장애·자료 없음·오래된 자료 사용이 명시된다.
- [ ] 필수 자료 부족 시 낙관적인 확정 판정이 생성되지 않는다.
- [ ] 근거·계산식·출처·자료 및 규칙 버전을 결과에서 확인할 수 있다.
- [ ] 같은 평가 실행의 화면과 보고서 수치가 일치한다.
- [ ] 사용자·기업 간 데이터와 보고서 접근이 차단된다.
- [ ] 데모 자료·실제 자료·가정값을 식별할 수 있다.
- [ ] 실제로 실행한 테스트와 미검증 항목을 구분해 기록한다.

## 13. 초기 개발 순서

1. 평가 범위·데이터 모델·표준 엑셀 양식 확정
2. 보유 API의 실제 조회 및 파일 버전·내용 검증
3. 판정 기준표와 근거 저장 구조 작성
4. 업로드 → 검증 → 평가 대상 선택 → 결과 흐름 구현
5. 바탕화면·창 관리자·책갈피 탭 구현
6. 검증된 데이터 범위부터 분석·보고서 연결
7. 홈 디자인·3D 연출 고도화
8. 실제 기업 사용에 필요한 계정·운영 정책 검증 후 적용

## 14. 변경 관리

변경 시 사용자 확정 요구사항, 구현 제안, 미확정 사항을 구분해 갱신한다. 기획 변경이 기존 분석 결과의 의미를 바꾸면 규칙·계산 버전을 올린다.

| 날짜 | 항목 | 결정·변경 내용 | 상태 |
|---|---|---|---|
| 2026-09-21 | 초기 명세 | 대화에서 확정된 화면 요구사항과 초기 개발 제안을 정리 | 초안 |
| 2026-09-22 | 신규 구축·디자인 분리 | 새 폴더에서 처음부터 구축하고, 홈 화면과 대시보드 앱의 디자인 시안을 분리 | 사용자 확정 |
| 2026-09-23 | 수정 예정 목록 반영 | Flask, 5개 평가 영역, 사용자 가중치, 격자 아이콘·휴지통·더블클릭, 보고서 다운로드 반영. 세부 미확정 사항 분리 | 요구사항 반영 |

문서가 커지면 업로드 규격, 판정 기준, 외부 자료 목록, 운영 정책을 별도 파일로 분리한다. 초기에는 이 문서를 기준 문서로 유지한다.
